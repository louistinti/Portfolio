dist
node_modules
package-lock.json
*.md

# Contenu éditorial : lignes de données volontairement compactes
# (une persona / feature par ligne). On laisse Prettier tranquille ici.
src/data

# Maquettes Renault : écrans extraits de l'export du projet de design.
# On garde le markup tel quel pour qu'il reste comparable à la source.
public/assets/renault/*.html
